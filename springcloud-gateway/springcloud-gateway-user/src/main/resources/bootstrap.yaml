server:
  port: 9000
spring:
  application:
    name: user
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        namespace: ${CFG_NS:}
        server-addr: ${CFG_ADDR:}
        register-enabled: true
      config:
        namespace: ${CFG_NS:}
        server-addr: ${CFG_ADDR:}
        file-extension: yaml
        extension-configs:
          - data-id: user.yaml
            refresh: true
